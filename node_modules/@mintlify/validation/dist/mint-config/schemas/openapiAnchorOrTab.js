import { z } from 'zod';
export const openapiAnchorOrTab = z
    .string()
    .trim()
    .nonempty()
    .refine((filePath) => filePath.startsWith('/') || filePath.startsWith('https'), "File path must start with '/' or 'https")
    .optional();
